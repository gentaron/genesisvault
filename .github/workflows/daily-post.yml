name: 🤖 Daily AI Blog Post

on:
  schedule:
    # 毎日午後5時15分(MYT/UTC+8)に実行 = UTC 9:15
    - cron: '15 9 * * *'
  workflow_dispatch: # 手動実行も可能

permissions:
  contents: write

jobs:
  create-post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate new blog post with AI
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node scripts/auto-post.mjs

      - name: Commit and push new post
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "Genesis Vault Bot 🤖"
          git add src/content/posts/*.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 自動投稿: $(date +'%Y-%m-%d') — AI生成ブログ" && git push)
